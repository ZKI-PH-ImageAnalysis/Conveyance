seed: 0

benchmark:
  dataset:
    name: fox
    mat_path: /path/to/fox.mat
  model:
    in_shape: [230]  
    num_classes: 2
    dropout: 0.1      
    backbone_dim: 512   
    inst_head_num_layers: 2
  train:
    epochs: 40
    lr: 0.001
    weight_decay: 0.0001
    max_grad_norm: 1.0
    save_dir: checkpoints/benchmark_fox
    device: cuda
  cv:
    enabled: true
    n_folds: 10
    runs: 5